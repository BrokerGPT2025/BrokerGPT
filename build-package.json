{
  "name": "brokergpt-build",
  "version": "1.0.0",
  "private": true,
  "description": "Temporary package.json only used during build for Render.com",
  "type": "commonjs",
  "scripts": {
    "build": "node custom-vite-build.cjs && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist"
  },
  "dependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "esbuild": "^0.19.0"
  }
}